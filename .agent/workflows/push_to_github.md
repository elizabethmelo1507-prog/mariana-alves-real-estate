---
description: Push changes to GitHub
---

1. Add all changes to staging
```bash
git add .
```

2. Commit changes with a descriptive message (replace "Update" with actual description)
```bash
git commit -m "Update application features and fixes"
```

3. Push to the main branch
```bash
git push origin main
```
